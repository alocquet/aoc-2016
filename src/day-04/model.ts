export class Room {
    constructor(public name: string, public id: number, public checksum: string) { }
}

export class LetterCount {
    constructor(public letter: string, public count: number) { }
}